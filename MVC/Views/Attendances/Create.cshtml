@model GigHub.Models.Attendance

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Attendance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GigId" class="control-label"></label>
                <select asp-for="GigId" class ="form-control" asp-items="ViewBag.GigId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AttendeeId" class="control-label"></label>
                <select asp-for="AttendeeId" class ="form-control" asp-items="ViewBag.AttendeeId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
